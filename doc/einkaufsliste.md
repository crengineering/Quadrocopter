# Einkaufsliste — moved

Procurement is system-level and now lives in the system-engineering repo:

- **`QuadSE/procurement/einkaufsliste.md`** — the master parts list (SSoT)
- **`QuadSE/procurement/reichelt_warenkorb.csv`** — the import-ready Reichelt cart

Owned by the system architect; updated in the `/ritem` gate flow when a
component decision is approved. Ordering is done by Chris.
(`einkaufsliste_reichelt.md` was already superseded by the master list and
is retired.)
